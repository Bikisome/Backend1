To add a comment in a Markdown file, you can use HTML comment syntax. Here is an example:

```markdown
<!-- This is a comment in Markdown -->
```

This comment will not be rendered in the final output.